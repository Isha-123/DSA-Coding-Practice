Approach 1:

max=a[0]
for(int i=0;i<n;i++){
    if(a[i]>max)
       max=a[i];
}


TC=0(n)  sp=(1)




Approach 2:
class Solution
{
public:
    int largest(vector<int> &arr, int n)
    
    {
        vector<int>ans;
        for(int i=0;i<n;i++){
            ans.push_back(arr[i]);
        }
        sort(ans.begin(),ans.end(),greater<int>());
        return ans[0];
        
    }
};

Tc=0(nlon)    sp=0(n)



